const Patterns = require('./Patterns');

// Patterns.petternFive(5);
// Patterns.petternSix(36);
// Patterns.petternSeven(100);

const ptrns = new Patterns(5);

ptrns.#getArrayFromLength(10);

// ptrns.patternOne();
// ptrns.patternTwo();
// ptrns.patternThree();
// ptrns.patternFour();
